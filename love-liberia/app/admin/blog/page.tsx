"use client";

import Link from "next/link";
import { useEffect, useState } from "react";

const categories = ["Dating Advice", "Relationships", "Love & Marriage", "Dating Safety", "Liberia", "Long Distance Relationships", "Dating Abroad", "Success Stories"];
type Article = { id: string; title: string; excerpt: string; content: string; category: string; status: string; publishedAt: string | null };
const blank = { title: "", excerpt: "", content: "", category: categories[0], status: "DRAFT", coverImage: "" };

export default function AdminBlogPage() {
  const [articles, setArticles] = useState<Article[]>([]); const [form, setForm] = useState(blank); const [editingId, setEditingId] = useState(""); const [error, setError] = useState(""); const [message, setMessage] = useState("");
  async function load() { const response = await fetch("/api/admin/blog", { cache: "no-store" }); const data = await response.json(); if (response.ok) setArticles(data.articles || []); else setError(data.error || "Unable to load articles."); }
  useEffect(() => { void load(); }, []);
  function change(field: keyof typeof blank, value: string) { setForm((current) => ({ ...current, [field]: value })); }
  async function save(event: React.FormEvent) { event.preventDefault(); setError(""); const response = await fetch("/api/admin/blog", { method: editingId ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(editingId ? { ...form, id: editingId } : form) }); const data = await response.json(); if (!response.ok) { setError(data.error || "Unable to save article."); return; } setMessage(editingId ? "Article updated." : "Article created."); setEditingId(""); setForm(blank); void load(); }
  async function remove(id: string) { if (!window.confirm("Delete this article?")) return; const response = await fetch(`/api/admin/blog?id=${encodeURIComponent(id)}`, { method: "DELETE" }); if (response.ok) { setMessage("Article deleted."); void load(); } }
  return <main className="min-h-screen bg-slate-100 px-5 py-8 text-slate-950 sm:px-8"><div className="mx-auto max-w-7xl"><Link href="/admin" className="text-sm font-bold text-rose-600">Admin dashboard</Link><h1 className="mt-2 text-3xl font-black">Love Liberia Magazine</h1><p className="mt-2 text-slate-600">Create, edit, publish, and archive magazine articles.</p>{error && <p className="mt-5 rounded-lg bg-red-50 p-3 text-red-700">{error}</p>}{message && <p className="mt-5 rounded-lg bg-emerald-50 p-3 text-emerald-700">{message}</p>}<div className="mt-8 grid gap-8 lg:grid-cols-[minmax(0,1fr)_minmax(0,1.2fr)]"><form onSubmit={save} className="rounded-2xl bg-white p-6 shadow-sm"><h2 className="text-xl font-black">{editingId ? "Edit article" : "New article"}</h2><div className="mt-5 space-y-4"><input value={form.title} onChange={(event) => change("title", event.target.value)} placeholder="Article title" className="min-h-11 w-full rounded-lg border border-slate-300 px-3" required /><select value={form.category} onChange={(event) => change("category", event.target.value)} className="min-h-11 w-full rounded-lg border border-slate-300 px-3">{categories.map((category) => <option key={category}>{category}</option>)}</select><select value={form.status} onChange={(event) => change("status", event.target.value)} className="min-h-11 w-full rounded-lg border border-slate-300 px-3"><option value="DRAFT">Draft</option><option value="PUBLISHED">Published</option></select><input value={form.coverImage} onChange={(event) => change("coverImage", event.target.value)} placeholder="Cover image URL (optional)" className="min-h-11 w-full rounded-lg border border-slate-300 px-3" /><textarea value={form.excerpt} onChange={(event) => change("excerpt", event.target.value)} placeholder="Short excerpt" rows={3} className="w-full rounded-lg border border-slate-300 p-3" required /><textarea value={form.content} onChange={(event) => change("content", event.target.value)} placeholder="Article content" rows={12} className="w-full rounded-lg border border-slate-300 p-3" required /><div className="flex gap-3"><button className="rounded-lg bg-rose-600 px-4 py-3 font-bold text-white">{editingId ? "Save changes" : "Create article"}</button>{editingId && <button type="button" onClick={() => { setEditingId(""); setForm(blank); }} className="rounded-lg border border-slate-300 px-4 py-3 font-bold">Cancel</button>}</div></div></form><section className="space-y-3">{articles.map((article) => <article key={article.id} className="rounded-xl bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><span className="text-xs font-black uppercase text-rose-600">{article.category} · {article.status}</span><h2 className="mt-1 text-xl font-black">{article.title}</h2><p className="mt-2 text-sm text-slate-600">{article.excerpt}</p></div><div className="flex gap-2"><button type="button" onClick={() => { setEditingId(article.id); setForm({ title: article.title, excerpt: article.excerpt, content: article.content, category: article.category, status: article.status === "PUBLISHED" ? "PUBLISHED" : "DRAFT", coverImage: "" }); }} className="rounded-lg border border-slate-300 px-3 py-2 text-sm font-bold">Edit</button><button type="button" onClick={() => void remove(article.id)} className="rounded-lg bg-red-600 px-3 py-2 text-sm font-bold text-white">Delete</button></div></div></article>)}</section></div></div></main>;
}
